{
  "name": "@gfxlabs/opencode-plugins-otel",
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "default": "./dist/index.mjs"
    }
  },
  "license": "(Unlicense OR MIT)",
  "files": [
    "dist",
    "!dist/tsconfig.tsbuildinfo",
    "otel.schema.json"
  ],
  "scripts": {
    "generate:schema": "tsx scripts/generate-schema.ts",
    "build": "npm run generate:schema && pkgroll",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.0.0"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
